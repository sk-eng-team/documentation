---
title: "Introduction"
description: "API documentation and reference guide"
---

## Welcome

This guide provides a comprehensive description of the API, including authentication methods, available endpoints, request and response formats, and recommended integration practices. It is intended to serve as a reference for developers to ensure reliable and efficient integration with the provided services.

## Base URL

All API requests must be directed to the following base URL:

<Tip>**Base URL**: https://api.external.hirednow.ai</Tip>

# Authentication

<Callout icon="key" color="#FFC107" iconType="regular">
  HiredNow APIs use API Key-based authentication.
</Callout>

<Steps>
   <Step title="Obtain API Credentials">
    Contact your HiredNow account representative or reach out to
    **devsupport@hirednow.com** to request API access credentials.
  </Step>
  <Step title="Set Up Authentication">
  Include your API key in all requests using the `Authorization` header: ```http
  Authorization: Bearer YOUR_API_KEY ``` Learn more in the
  [Introduction](/overview/introduction) section.

  <CodeGroup>

```bash cURL
curl -X GET "https://api.external.hirednow.ai/v1/campaigns" \
          -H "Authorization: Bearer <YOUR_API_KEY>" \
          -H "Content-Type: application/json"
```

```http HTTP theme={null}
GET /campaigns HTTP/1.1
Host: api.external.hirednow.ai/[path]
Authorization: Bearer <YOUR_API_KEY>
Content-Type: application/json
```

</CodeGroup>

 </Step>

</Steps>
````

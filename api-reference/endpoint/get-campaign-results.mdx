---
title: "GET Campaign Results"
openapi: "GET /campaigns/results"
description: "Retrieves summarized performance metrics and outcomes for a specific recruitment campaign, including total applicants, interviews, and hires."
---

### GET /campaigns/results

<Note>
  Retrieve campaign performance data such as number of applicants, interviews, and hires for a specific `campaignId`.
</Note>

<Warning>
  **Base URL**: https://api.hirednow.ai/v1
</Warning>

#### Request Endpoint:

```http
GET /campaigns/results?campaignId={campaignId}
```

#### Query Parameters:

| Parameter    | Type     | Required | Description                   |
| ------------ | -------- | -------- | ----------------------------- |
| `campaignId` | `string` | ✅        | The unique ID of the campaign |

#### Example Request:

<Tabs>
  <Tab title="HTTP">
    ```http
    GET /campaigns/results?campaignId=c1a2b3 HTTP/1.1 
    Host: api.hirednow.ai 
    Authorization: Bearer <YOUR_API_KEY> 
    Content-Type: application/json 
    ```
  </Tab>
  <Tab title="JSON">
    ```json
    { 
      "method": "GET", 
      "url": "https://api.hirednow.ai/v1/campaigns/results?campaignId=c1a2b3",
      "headers": { 
        "Authorization": "Bearer <YOUR_API_KEY>", 
        "Content-Type": "application/json" 
      } 
    } 
    ```
  </Tab>
</Tabs>

#### Example Response:

```json
  {
    "data": [
      {
        "campaignId": "c1a2b3",
        "name": "Marketing Intern 2025",
        "totalApplicants": 240,
        "interviewed": 75,
        "hired": 10,
        "status": "active",
        "updatedAt": "2025-10-10T14:25:00Z"
      },
    ]
  }
```

#### Response Codes:

| Code    | Description            |
| ------- | ---------------------- |
| **200** | ✅ Success              |
| **400** | ❌ Missing `campaignId` |
| **401** | 🔒 Unauthorized        |
| **404** | ❗ Campaign not found   |
| **500** | ⚠️ Server error        |

<Note>
  Ensure that the `campaignId` belongs to a campaign within your organization. Unauthorized access or invalid campaign IDs will result in a 401 or 404 error.
</Note>
---
title: "List"
openapi: "GET /v1/campaigns"
---

<Note>
  The `type` parameter returned for each campaign determines which results
  endpoint to use. Different campaign types require their corresponding results
  endpoints for fetching data.
</Note>

---
title: "GET Campaigns Endpoint"
description: "Retrieves a list of all active and archived recruitment campaigns associated with a specific company. Each campaign includes details such as campaign ID, name, status, and creation date."
---

### GET /campaigns

<Note>
  Retrieve all campaigns associated with a specific `companyId`.
</Note>

<Warning>
  **Base URL**: https://api.hirednow.ai/v1
</Warning>

#### Request Endpoint:

```http
GET /campaigns?companyId={companyId}
```

#### Query Parameters:

| Parameter   | Type     | Required | Description                  |
| ----------- | -------- | -------- | ---------------------------- |
| `companyId` | `string` | ✅        | The unique ID of the company |

#### Example Request:

<Tabs>
  <Tab title="HTTP">
    ```http
    GET /campaigns?companyId=12345 HTTP/1.1 
    Host: api.hirednow.ai 
    Authorization: Bearer <YOUR_API_KEY> 
    Content-Type: application/json 
    ```
  </Tab>
  <Tab title="JSON">
    ```json
    { 
      "method": "GET", 
      "url": "https://api.hirednow.ai/v1/campaigns?companyId=12345",
      "headers": { 
        "Authorization": "Bearer <YOUR_API_KEY>", 
        "Content-Type": "application/json" 
      } 
    } 
    ```
  </Tab>
</Tabs>

#### Example Response:

```json
  {
    "data": [
      {
        "campaignId": "c1a2b3",
        "name": "Marketing Intern 2025",
        "status": "active",
        "createdAt": "2025-09-15T10:45:00Z"
      },
      {
        "campaignId": "c2a3b4",
        "name": "Software Engineer Hiring Drive",
        "status": "closed",
        "createdAt": "2025-06-02T09:30:00Z"
      }
    ]
  }
```

#### Response Codes:

| Code    | Description           |
| ------- | --------------------- |
| **200** | ✅ Success             |
| **400** | ❌ Missing `companyId` |
| **401** | 🔒 Unauthorized       |
| **500** | ⚠️ Server error       |

<Note>
  Ensure your API key is valid and associated with the same companyId. Unauthorized or expired keys will return a 401 error.
</Note>
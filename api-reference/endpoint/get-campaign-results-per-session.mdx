---
title: "GET Campaign Results Per Session"
openapi: "GET /campaigns/results-per-session"
description: "Fetches detailed performance insights for each session of a campaign, including metrics such as applicants, interviews, and hires per session."
---

### GET /campaigns/results-per-session

<Note>
  Retrieve granular session-based performance data for a specific `campaignId`, useful for tracking trends across recruitment sessions.
</Note>

<Warning>
  **Base URL**: https://api.hirednow.ai/v1
</Warning>

#### Request Endpoint:

```http
GET /campaigns/results-per-session?campaignId={campaignId}
```

#### Query Parameters:

| Parameter    | Type     | Required | Description                   |
| ------------ | -------- | -------- | ----------------------------- |
| `campaignId` | `string` | ✅        | The unique ID of the campaign |

#### Example Request:

<Tabs>
  <Tab title="HTTP">
    ```http
    GET /campaigns/results-per-session?campaignId=c1a2b3 HTTP/1.1 
    Host: api.hirednow.ai 
    Authorization: Bearer <YOUR_API_KEY> 
    Content-Type: application/json 
    ```
  </Tab>
  <Tab title="JSON">
    ```json
    { 
      "method": "GET", 
      "url": "https://api.hirednow.ai/v1/campaigns/results-per-session?campaignId=c1a2b3",
      "headers": { 
        "Authorization": "Bearer <YOUR_API_KEY>", 
        "Content-Type": "application/json" 
      } 
    } 
    ```
  </Tab>
</Tabs>

#### Example Response:

```json
  {
    "data": [
      {
        "sessionId": "s101",
        "sessionDate": "2025-09-20",
        "applicants": 85,
        "interviewed": 25,
        "hired": 4
      },
      {
        "sessionId": "s102",
        "sessionDate": "2025-10-05",
        "applicants": 155,
        "interviewed": 50,
        "hired": 6
      }
    ]
  }
```

#### Response Codes:

| Code    | Description                      |
| ------- | -------------------------------- |
| **200** | ✅ Success                        |
| **400** | ❌ Missing `campaignId`           |
| **401** | 🔒 Unauthorized                  |
| **404** | ❗ Campaign or sessions not found |
| **500** | ⚠️ Server error                  |

<Note>
  Use this endpoint to monitor recruitment effectiveness over multiple campaign sessions and identify patterns in candidate engagement.
</Note>